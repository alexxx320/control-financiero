[build]
command = "npm install && npm run build"

[deploy]
startCommand = "npm run start:prod"

[env]
NODE_ENV = "production"
PORT = "$PORT"

# Indicar que el código está en el directorio backend
[source]
path = "./backend"
